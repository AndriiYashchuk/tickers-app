import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'tickers-app',
  description: 'Home page',
}

const Page = () => {
  return (
  <>
      <h1>Hello, Index page!</h1>
  </>);
}

export default Page;


